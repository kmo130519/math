#ifndef MULTIPLICATION_HPP
#define MULTIPLICATION_HPP

/**
 * @brief 두 정수의 곱을 계산하는 함수.
 *
 * 이 함수는 두 개의 정수를 인자로 받아서 그 곱을 반환합니다.
 * @param a 첫 번째 정수
 * @param b 두 번째 정수
 * @return 두 정수의 곱
 */
int multiplication(int a, int b);

#endif // MULTIPLICATION_HPP
